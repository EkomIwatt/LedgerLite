# LedgerLite

Personal expense tracker — log expenses (amount, category, date, note), set
per-category monthly budgets, and visualize spending with a category pie chart,
a month-over-month bar chart, and a "budget remaining" gauge. Email/password
signup so each user's data is private.

A Swarm multi-agent project. Run `/swarm-initialiser` here to plan the parallel build.
